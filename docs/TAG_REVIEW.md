# 태그 삭제 후보 검토
> **임시 문서다.** 삭제를 적용한 뒤 이 파일은 지운다.
## 사용법
**남기고 싶은 태그의 줄을 지우세요. 이 파일에 남아 있는 줄이 삭제 대상입니다.**
다 고른 뒤 "적용해줘"라고 말씀하시면 `docs/TAG.md`에서 남은 태그들을 지우고, 개수·번호를 다시 매기고, `SCRIPT.md`·`CLAUDE.md` 등의 수치까지 맞춥니다.
## 등급 표시
- `[A]` 신입 범위 밖 (54개) — 삭제 권장
- `[B]` 경계선 (58개) — 심화지만 신입 면접에 실제로 나온다. `※ 남기기 권장` 표시가 붙은 건 남기는 쪽을 권합니다
- `[C]` 중복·모호 (38개) — 난이도가 아니라 사전 품질 문제. 다른 태그에 흡수되거나 무엇을 재는지 불분명

현재 사전 **324개** 중 후보 **150개**. 전부 지우면 174개가 남습니다.

---

## DB
현재 40개 · 후보 14개 · 전부 지우면 26개 남음

- **언두 로그와 리두 로그** `[A]` — InnoDB 내부 로그 구조. 신입 면접에 거의 안 나온다
- **저장 프로시저와 트리거** `[A]` — 레거시 영역. 요즘 신입 실무에서 거의 안 쓴다
- **벌크 연산** `[A]` — 대량 처리 튜닝 세부. 실무 경험이 필요하다
- **백업과 복구** `[A]` — DBA·운영 영역
- **CAP 정리** `[B]` — RDB와 NoSQL 비교로 종종 나온다
- **인덱스 미적용 사례** `[B]` — 실무적이지만 유용. 인덱스 이해도를 재기 좋다
- **스키마 설계** `[C]` — 범위가 모호. 정규화·키와 제약조건과 겹친다
- **데이터 타입 선택** `[C]` — 무엇을 재는 태그인지 불분명
- **집합 연산** `[C]` — SQL 기본 문법에 흡수 가능

## NETWORK (네트워크)
현재 40개 · 후보 17개 · 전부 지우면 23개 남음

- **gRPC** `[A]` — 신입 경험 밖의 프로토콜
- **SSE** `[A]` — 실무에서도 드물게 쓴다
- **스티키 세션** `[A]` — 로드밸런서 운영 세부
- **TIME_WAIT** `[A]` — TCP 상태 전이 심화
- **인증서 체인** `[A]` — TLS 심화. HTTPS와 TLS로 충분하다
- **MTU와 단편화** `[A]` — 네트워크 계층 심화
- **헬스 체크** `[A]` — 인프라 운영 영역
- **ARP** `[B]` — CS 기초로 나오긴 하나 백엔드에겐 살짝 멀다
- **소켓 통신** `[B]` — TCP 이해도와 함께 물어보는 편
- **무상태성** `[C]` — REST API에 흡수 가능

## ALGORITHM (알고리즘)
현재 40개 · 후보 17개 · 전부 지우면 23개 남음

- **분기 한정** `[A]` — 이론 영역. 코테에 안 나온다
- **근사와 휴리스틱** `[A]` — 이론 영역
- **P와 NP** `[A]` — 계산 이론. 신입 면접·코테 범위 밖
- **구간 합 쿼리** `[A]` — 세그먼트 트리급 자료구조가 필요하다
- **파라메트릭 서치** `[B]` — 이진 탐색 응용. 코테 준비생은 배우는 편
- **사이클 판별** `[B]` — 그래프 기초 응용
- **최장 증가 부분 수열** `[B]` — DP 대표 문제  ※ 남기기 권장
- **배낭 문제** `[B]` — DP 대표 문제  ※ 남기기 권장
- **비트마스킹** `[B]` — 코테 심화 유형
- **정렬 기준 설계** `[C]` — 무엇을 재는 태그인지 불분명
- **제자리 정렬** `[C]` — 정렬 알고리즘·정렬 안정성에 흡수 가능
- **점근 표기법** `[C]` — 시간 복잡도와 중복
- **재귀와 반복 변환** `[C]` — 재귀와 중복
- **메모이제이션** `[C]` — 동적 프로그래밍과 중복

## DATA_STRUCTURE (자료구조)
현재 40개 · 후보 21개 · 전부 지우면 19개 남음

- **세그먼트 트리** `[A]` — 고급 자료구조. 신입 코테 범위 밖
- **펜윅 트리** `[A]` — 고급 자료구조
- **스킵 리스트** `[A]` — 거의 다루지 않는다
- **블룸 필터** `[A]` — 확률적 자료구조. 실무 심화
- **AVL 트리** `[A]` — 균형 이진 트리로 충분하다
- **B-트리** `[B]` — B+트리와 함께 인덱스 설명에 쓰인다
- **힙 구현** `[C]` — 힙과 중복
- **개방 주소법과 체이닝** `[C]` — 해시 충돌과 중복(충돌 해결법이 곧 이것)
- **해시 함수 설계** `[C]` — 해시 테이블·해시 충돌과 중복
- **정렬된 컬렉션** `[C]` — 셋과 맵과 중복
- **불변 자료구조** `[C]` — LANGUAGE의 불변 객체와 겹친다
- **문자열 자료구조** `[C]` — 모호. 트라이와 겹친다
- **자료구조 선택 기준** `[C]` — 메타적이고 범위가 모호
- **방향 그래프와 무방향 그래프** `[C]` — 그래프 표현에 흡수 가능
- **가중치 그래프** `[C]` — 그래프 표현에 흡수 가능

## OS (운영체제)
현재 40개 · 후보 17개 · 전부 지우면 23개 남음

- **TLB** `[A]` — MMU 내부. 신입 면접에 거의 없다
- **우선순위 역전** `[A]` — 실시간 시스템 영역
- **모니터** `[A]` — 동기화 심화. 뮤텍스와 세마포어로 충분하다
- **아이노드** `[A]` — 파일시스템 내부
- **디스크 스케줄링** `[A]` — 커널 내부. 요즘 면접에 안 나온다
- **원자적 연산** `[B]` — 동시성 이해에 필요하지만 다소 심화
- **사용자 스레드와 커널 스레드** `[B]` — 스레드 모델로 나오는 편
- **시그널** `[B]` — 프로세스 제어 기초~중급
- **fork와 exec** `[B]` — 프로세스 생성으로 자주 나온다  ※ 남기기 권장
- **하드링크와 심볼릭 링크** `[B]` — 리눅스 기초. 백엔드에겐 유용
- **메모리 관리** `[C]` — 가상 메모리·페이징의 상위 개념. 범위가 모호

## DESIGN_PATTERN (디자인패턴)
현재 40개 · 후보 21개 · 전부 지우면 19개 남음

- **인터프리터** `[A]` — 실무에서 거의 안 쓴다
- **방문자** `[A]` — 신입이 접할 일이 없다
- **중재자** `[A]` — 신입이 접할 일이 없다
- **메멘토** `[A]` — 신입이 접할 일이 없다
- **플라이웨이트** `[A]` — 신입이 접할 일이 없다
- **브리지** `[A]` — 신입이 접할 일이 없다
- **프로토타입** `[A]` — 자바에서 clone 이슈로만 잠깐 나온다
- **책임 연쇄** `[B]` — 필터·인터셉터 비유로 설명하기 좋다  ※ 남기기 권장
- **상태** `[B]` — 전략 패턴과 비교로 나온다  ※ 남기기 권장
- **커맨드** `[B]` — 언급 빈도가 낮지 않다
- **패턴 분류** `[C]` — GoF와 중복(생성·구조·행위 분류가 곧 그것)
- **패턴 선택 기준** `[C]` — 무엇을 재는 태그인지 불분명
- **객체 생성 책임 분리** `[C]` — 모호. 팩토리 메서드와 겹친다
- **도메인 모델** `[C]` — 범위가 모호
- **캡슐화와 정보 은닉** `[C]` — LANGUAGE의 객체지향 4대 특성과 겹친다
- **리팩터링** `[C]` — 디자인패턴 카테고리 성격과 맞지 않는다
- **테스트 용이성** `[C]` — 디자인패턴보다 GENERAL_CS 성격

## LANGUAGE (언어/Java)
현재 40개 · 후보 16개 · 전부 지우면 24개 남음

- **sealed 클래스** `[A]` — 최신 문법. 신입이 쓸 일이 드물다
- **가변 인수** `[A]` — 문법 세부. 중요도가 낮다
- **var와 타입 추론** `[C]` — 문법 세부. 중요도가 낮다
- **내부 클래스** `[C]` — 중요도가 낮다
- **정적 팩토리 메서드** `[C]` — DESIGN_PATTERN의 팩토리 메서드와 겹친다

## GENERAL_CS (기타 CS)
현재 44개 · 후보 27개 · 전부 지우면 17개 남음

- **클린 코드와 기술 부채** `[C]` — 범위가 모호. 문항으로 만들기 어렵다
- **코드 리뷰** `[C]` — 객관식·서술형 문항으로 만들기 애매하다
- **로깅과 모니터링** `[C]` — 운영 성격이 강하고 범위가 모호
- **JSON과 데이터 포맷** `[C]` — LANGUAGE의 직렬화와 겹친다

---

## 후보에 넣지 않은 태그
위에 없는 태그는 신입 면접·코테 빈출 핵심으로 판단해 후보에서 제외했습니다. 그중에도 지우고 싶은 게 있으면 이 파일 아래에 그냥 적어 주세요.

```
(여기에 추가 삭제할 태그를 적으세요)
안티패턴

```
